<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dsm.iriscalendar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".IrisCalendarApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="m">
        <activity android:name=".ui.modifyFixedSchedule.ModifyFixedScheduleActivity" />
        <activity android:name=".ui.modifySchedule.ModifyScheduleActivity" />
        <activity android:name=".ui.addFixedSchedule.AddFixedScheduleActivity" />

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="true" />

        <activity android:name=".ui.addSchedule.AddScheduleActivity" />
        <activity android:name=".ui.category.CategoryActivity" />
        <activity android:name=".ui.reTimeSet.ReTimeSetActivity" />
        <activity android:name=".ui.timeSet.TimeSetActivity" />
        <activity android:name=".ui.signUp.SignUpActivity" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>